spring:
  datasource:
    url: jdbc:h2:mem:eventia-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    root: WARN
    org.springframework: WARN
    org.hibernate: WARN

